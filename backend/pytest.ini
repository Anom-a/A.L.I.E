[pytest]
# The domain package lives at backend/domain; put backend/ (this dir) on the
# import path so `import domain` works without installing the package.
pythonpath = .
testpaths = tests
addopts = -ra
