[pytest]
# The backend packages live at backend/<layer>; put backend/ (this dir) on the
# import path so `import domain`, `import adapters`, `import infrastructure`
# work without installing the package.
pythonpath = .
testpaths = tests
addopts = -ra --strict-markers
markers =
    integration: exercises a real external API; skipped automatically when credentials are absent
