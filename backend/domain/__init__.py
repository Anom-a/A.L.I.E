"""Domain layer for the Research Agent.

This package is pure Python and depends only on the standard library. It must
not import from application, adapters, or infrastructure layers, nor from any
third-party framework (FastAPI, LangGraph, OpenAI, httpx, Pydantic, database
drivers). See README.md for the Clean Architecture dependency rule.
"""
